import { color } from '@/styles/theme'
import { css } from '@emotion/react'

export const styles = {
  label: css`
    margin: 0 auto;
    background-color: ${color.white};
    color: ${color.red};
  `,
  input: css``,
}
