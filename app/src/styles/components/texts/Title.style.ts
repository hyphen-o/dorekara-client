import { color } from '@/styles/theme'
import { css } from '@emotion/react'

export const styles = {
  title: css`
    font-size: 23px;
    color: ${color.darkgray};
    border-bottom: solid;
    text-align: center;
  `,
}
