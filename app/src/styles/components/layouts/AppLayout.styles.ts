import { color } from '@/styles/theme'
import { css } from '@emotion/react'

export const styles = {
  header: css`
    background-color: ${color.red};
    height: 254px;
    padding-top: 30px;
  `,
}
