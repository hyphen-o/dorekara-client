import { css } from "@emotion/react";
import { color } from "../theme";

export const styles = {
  login: css`
  `,
  wrapper: css`
  `,
  text: css`
    font-size: 12px;
    color: ${color.darkgray};
  `
}