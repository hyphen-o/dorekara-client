import { css } from '@emotion/react'
import { color } from '../theme'

export const styles = {
  wrapper: css``,
  text: css`
    font-size: 50px;
    text-align: center;
    color: ${color.red};
  `,
}
