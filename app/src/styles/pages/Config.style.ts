import { css } from '@emotion/react'
import { color } from '../theme'

export const styles = {
  wrapper: css`
    margin: 230px auto;
    color: ${color.darkgray};
  `,
  button: css`
    margin-top: 20px;
  `,
}
